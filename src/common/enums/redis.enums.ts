export enum UserVersionContextEnum {
  USER = "User",
  PROFILE = "Profile",
  SETTINGS = "Settings",
  STATS = "Stats",
  WHOLE_PROFILE = "Whole_Profile",
}

export enum PostContextEnum {
  OWNER = "Owner",
  FOLLOWER = "Follower",
  PUBLIC = "Public",
}
